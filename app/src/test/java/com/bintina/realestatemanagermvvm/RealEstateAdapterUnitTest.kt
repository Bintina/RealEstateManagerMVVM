package com.bintina.realestatemanagermvvm

import com.bintina.realestatemanagermvvm.realestate.model.ListingItem
import com.bintina.realestatemanagermvvm.realestate.view.adapter.Adapter
import org.junit.After
import org.junit.Before
import org.junit.Test


class RealEstateAdapterUnitTest {
/*    @Mock(relaxed = true)
    lateinit var listener: OnRealEstateClickedListener

    lateinit var  adapter: com.bintina.realestatemanagermvvm.realestate.view.adapter.Adapter

    @Before
    fun set_up(){
        MockKAnnotations.init(this)
        adapter = Adapter()
        adapter.listener = listener
    }

    val mockRealEstate = MockRealEstate(4)
    val expectedResults = mockRealEstate.mockRealEstateList

    @After
    fun tear_down(){
        clearAllMocks()
    }

    @Test
    fun get_item_count_should_return_correct_item_count(){
        adapter.realEstateList = expectedResults

        val count = adapter.itemCount

        assert(count == 4)
    }

    @Test
    fun on_bind_view_holder_should_bind_to_view_holder(){
        val viewHolder: Adapter.ItemViewHolder = mockk(relaxed = true)
        val position = 0
        val realEstate: ListingItem = mockk(relaxed = true)

        adapter.realEstateList = listOf(realEstate)
        adapter.onBindViewHolder(viewHolder, position)

        verify(exactly = 1) {viewHolder.bind(realEstate, listener)}
    }*/
}