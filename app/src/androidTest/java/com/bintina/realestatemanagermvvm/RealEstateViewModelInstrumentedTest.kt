package com.bintina.realestatemanagermvvm

import androidx.room.Room
import androidx.test.ext.junit.runners.AndroidJUnit4
import com.bintina.realestatemanagermvvm.realestate.model.ListingItem
import org.junit.After
import org.junit.Before
import org.junit.Rule
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class RealEstateViewModelInstrumentedTest {
/*    private var database: SaveRealEstateDatabase? = null

    @Rule
    @JvmField
    var instantTaskExecutorRule = InstantTaskExecutorRule()

    @Before
    @Throws(Exception::class)
    fun initDb(){
        this.database = Room.inMemoryDatabaseBuilder(
            getApplicationContext(),
            SaveRealEstateDatabase::class.java
        )
            .allowMainThreadQueries()
            .build()
    }

    @After
    @Throws(Exception::class)
    fun closeDb(){
        database?.close()
    }

    private val REALESTATE_ID: Long = 10
    private val REALESTATE_DEMO =
        ListingItem(REALESTATE_ID,
            TODO("fill in properties from class"))*/
}